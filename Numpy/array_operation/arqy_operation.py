import numpy as np 

# addition

first_array = np.array([2, 4, 6, 8])
second_array = np.array([10, 20, 25, 15])
# using the add function
result1 = np.add(first_array, second_array)
# using the + operator
result = first_array + second_array
print(result)
print(result1)

